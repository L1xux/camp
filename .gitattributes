# 줄바꿈을 LF 로 고정한다. Windows 로컬과 CI 의 리눅스가 같은 포맷 검사 결과를 내야 한다.
* text=auto eol=lf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
*.ico binary

# 래퍼 스크립트는 실행 환경의 줄바꿈을 따른다.
gradlew text eol=lf
gradlew.bat text eol=crlf
