# CAMP 레포 규약

전역 개발 규율은 `C:/Users/oo/file/project/DEVELOPMENT_CYCLE.md` 를 따른다.
이 문서는 이 레포에서만 적용되는 규약이다. UiJin 의 직접 지시가 이 문서보다 우선한다.

## 글쓰기 규칙 (문서, 이슈, PR, 커밋, 주석, 답변)

1. **짧게 쓴다.** 한 문단이면 될 것을 세 문단으로 늘리지 않는다.

2. **업계 표준 용어를 쓴다.** 지어낸 요약어 금지.

   | 쓰지 말 것 | 쓸 것 |
   |---|---|
   | 두 겹으로 강제, 강제 수단 두 가지 | 의존성 규칙, 컴파일 에러, ArchUnit 테스트 |
   | 테스트의 테스트 | 규칙이 위반을 실제로 잡는지 확인 |
   | 관측 3층, 영향 5축, 컷 1순위 | 항목을 그대로 나열 |

3. **무엇에 대한 이야기인지 먼저 밝힌다.** "규칙을 어기면" (X) → "의존성 규칙을 어기면" (O).

4. **결론에 이유를 한 문장 붙인다.** "검사가 두 개다" 만 쓰면 왜 두 개인지 알 수 없다.

5. **내부 문서 절 번호를 인용하지 않는다.** "(사이클 1-10)" 대신 내용을 한 문장으로 옮긴다.

6. **가운뎃점(`·`) 금지.** 쉼표를 쓴다.

7. **코드 주석은 한 줄.** 배경 설명이 필요하면 ADR 이나 이슈에 쓴다.

## 이슈 착수부터 머지까지

**이슈의 완료 조건을 전부 충족하기 전에는 머지를 요청하지 않는다.**

1. **착수 전에 체크리스트를 만들어 UiJin 에게 보여주고 승인받는다.**
   - 옮길 대상은 **요구사항, 예외 케이스, 테스트 케이스, 완료 조건** 네 가지 전부다.
   - **원문 그대로** 옮긴다. 요약하거나 문장을 다듬지 않는다.
     체크박스를 붙이려고 `- ` 를 `- [ ] ` 로 바꾸는 것만 허용한다.
   - 보여주는 이유는 Claude 가 이슈를 제대로 읽었는지 UiJin 이 확인하기 위해서다.
     승인 전에는 코드를 쓰지 않는다.
   - 옮기면서 빠뜨린 항목은 끝까지 안 하게 된다. 개수를 세어 함께 보고한다.

2. **항목마다 증거를 붙인다.** 실행한 명령과 출력 원문을 그대로. 요약 문장은 증거가 아니다.

3. **못 지킨 항목은 "알려진 한계" 로 넘기지 않는다.** Claude 가 임의로 유예할 수 없다.
   해당 항목을 명시하고 UiJin 의 판단을 받는다. 유예가 승인되면 후속 이슈 번호를 남긴다.

4. **이슈 본문이 사실과 다르면 UiJin 에게 보고한다.** 이슈는 UiJin 이 정한 명세다.
   Claude 가 임의로 수정하지 않는다. 무엇이 어떻게 다른지와 확인한 근거를 제시하고,
   고칠지 말지와 어떻게 고칠지는 UiJin 이 정한다. 이슈, ADR, PLAN 등 명세 성격의 문서 전부
   같은 규칙을 따른다.

5. **PR 을 올린 뒤 이슈의 체크박스를 체크한다.** 끝낸 항목을 `- [x]` 로 바꿔 이슈 화면에서
   진행 상황이 바로 보이게 한다. 체크박스 체크는 진행 기록이므로 4번의 "임의 수정 금지" 에
   해당하지 않는다. 다만 항목의 문구를 바꾸는 것은 금지다.

6. **머지 요청 시 체크리스트 전체를 제시한다.** 각 항목의 상태(완료 / 미완료 / 유예 요청)를
   숨기지 않고 전부 보여준다.

## GitHub 에 올리기 전 승인

이슈 코멘트, 이슈 본문 수정, 이슈 상태 변경(열기 / 닫기), PR 생성과 수정은 **초안을 먼저
보여주고 UiJin 의 승인을 받은 뒤** 올린다. 한번 올라간 글은 다른 사람이 읽는 기록이 된다.

체크박스 체크는 예외다 (진행 기록이므로 바로 반영한다).
레포 커밋과 푸시도 이 규칙과 별개다.

## 검증 결과를 기록하는 방법

**손으로 요약해서 쓰지 않는다.** "테스트 8건 통과" 같은 문장은 누구나 타이핑할 수 있어서
증거가 되지 않는다. 다음 순서로 증거를 남긴다.

1. **CI 상태 체크가 1순위다.** 빌드, 테스트, 정적 분석, 골든셋 평가는 CI 가 돌린 결과를
   근거로 삼는다. PR 화면의 체크 표시가 증거이므로 본문에 따로 옮겨 적지 않는다.
2. **CI 가 돌릴 수 없는 것만 PR 본문에 남긴다.** 실행 화면, 수동 검증, 일부러 위반을
   넣어본 결과 등이 여기 해당한다.
3. **남길 때는 터미널 출력을 그대로 붙인다.** 요약하거나 바꿔 쓰지 말고 실행한 명령과
   출력을 코드 블록에 원문 그대로 넣는다. 유리한 줄만 골라 붙이지 않는다.
4. **화면이 필요한 것은 스크린샷으로 남긴다.** UI 동작, 브라우저 화면, 대시보드처럼
   텍스트로 옮기면 의미가 죽는 것은 UiJin 이 스크린샷을 찍어 PR 에 첨부한다.
   Claude 는 스크린샷을 찍을 수 없으므로, 필요하면 무엇을 찍어야 하는지 알린다.
5. **성능 수치는 측정 스크립트를 레포에 남긴다.** 일회성으로 재보고 숫자만 적으면
   다음에 재현할 수 없다.

## 스택

`gradle/libs.versions.toml` 이 버전의 단일 출처다. 버전을 다른 곳에 직접 쓰지 않는다.

- Java 21 (Temurin), Gradle 8.14.5, Spring Boot 3.5.16, Spring AI 1.1.8, Oracle 23ai
- 조합 근거: `docs/adr/002-빌드-툴체인-버전조합.md`

## 모듈 구조

```
domain      <- application <- adapter-web / adapter-mcp / adapter-batch
                           <- infra
```

의존성 규칙 두 가지:

- `domain` 에는 라이브러리를 선언하지 않는다 (JDK 표준 라이브러리만).
- `adapter-web`, `adapter-mcp`, `adapter-batch` 는 `infra` 를 컴파일 시점에 참조하지 않는다.
  `adapter-web` 이 `infra`, `adapter-mcp`, `adapter-batch` 를 `runtimeOnly` 로 조립한다.

위반하면 컴파일 에러가 나고, `build.gradle` 을 고쳐 우회하면 `ArchitectureTest` 가 실패한다.

## 명령어

```bash
export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.9.10-hotspot"
./gradlew build                              # 컴파일 + 테스트 + 아키텍처 검사
./gradlew :adapter-web:bootRun               # http://127.0.0.1:8280/actuator/health
./gradlew resolveAndLockAll --write-locks    # 의존성 변경 후 잠금 갱신
```

포트: 백엔드 8280, 프론트 5280, Oracle 1522. 서버 바인딩 주소는 `0.0.0.0` 을 명시한다
(기본값에 의존하면 이 PC 에서 기동이 실패한 이력이 있다. `docs/ENVIRONMENT.md` 참조).
